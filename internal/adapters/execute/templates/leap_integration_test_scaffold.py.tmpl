
from code_loader.contract.datasetclasses import PreprocessResponse
from code_loader.inner_leap_binder.leapbinder_decorators import (
    tensorleap_preprocess,
    tensorleap_integration_test,
)


@tensorleap_preprocess()
def preprocess_func():
    """Placeholder – Concierge will implement this in a later step."""
    return []


@tensorleap_integration_test()
def integration_test(sample_id: str, preprocess: PreprocessResponse) -> None:
    """Baseline integration-test scaffold generated by Concierge."""
    return None


if __name__ == "__main__":
    responses = preprocess_func()
    for subset in responses:
        for sample_id in subset.sample_ids[:5]:
            integration_test(sample_id, subset)
